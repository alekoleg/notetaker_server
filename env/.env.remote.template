# Remote overlay — overrides env/.env.shared for production. Copy to env/.env.remote.
# Only keys that differ from shared belong here.

SERVER_URL="https://api.biblenotetaker.app/api/parse"
APNS_PRODUCTION="true"
