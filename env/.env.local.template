# Local overlay — overrides env/.env.shared for local dev. Copy to env/.env.local.
# Only keys that differ from shared belong here.

SERVER_URL="http://localhost:9090/api/parse"
APNS_PRODUCTION="false"
